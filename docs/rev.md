## typeof: It is an Operator that evaluates an operand and returns a string indicating its data type.

## trim(): it is a method which removes whitespace from both ends of a string.
   example: const greet = "    hello!    "
            const clean = greet.trim();
            console.log("clean");
            //output will be "hello!"
## Javascript method is simply a function that belongs to an object, while a regular funciton stands on its own, a method is stored as a property inside an object and is typically used to define the behaviours or actions that the object can perform. We can call a method using a dot notation (eg: object.methodname())

